/**
 * calculateNumber - Rounds two numbers and returns their sum
 * @a: first number
 * @b: second number
 *
 * Return: the sum of rounded a and b
*/

function calculateNumber(a, b) {
  return Math.round(a) + Math.round(b);
}

module.exports = calculateNumber;
