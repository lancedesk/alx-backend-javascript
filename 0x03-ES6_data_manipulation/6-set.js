/**
 * Creates a Set from an array.
 *
 * Parameters:
 * - array: An array from which to create the Set.
 *
 * Returns:
 * - A Set containing unique elements from the input array.
 */

function setFromArray(array) {
  return new Set(array);
}

export default setFromArray;
